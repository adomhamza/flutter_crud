class NotesForListing {
  String noteID;
  String noteTitle;
  DateTime createDateTime;
  DateTime lastEditDateTime;

  NotesForListing(
      {this.noteID,
      this.noteTitle,
      this.createDateTime,
      this.lastEditDateTime});
}
